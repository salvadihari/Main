pipeline {
    agent any
    stages{
        stage('Build'){
	    steps {
		sh 'maven clean package'
		 }
		post {
		   success {
			echo "now Archiving"
			archiveArtifacts artifacts: '**/target/*.jar'
		   }
		}
	}
}
}

